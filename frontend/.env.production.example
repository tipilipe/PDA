# Caminho base quando hospedado em subpasta (ex.: /sistema/). Para subdomínio use "/".
VITE_BASE_PATH=/sistema/
# URL do backend (ex.: https://api.shipstore.com.br). Se vazio, usa caminhos relativos.
VITE_API_BASE=https://api.shipstore.com.br